<div class="comment">
  <p><strong><%= comment.user&.email || 'Usuário Anônimo' %></strong> comentou:</p>
  <p><%= comment.body %></p>
  <p class="text-muted"><%= comment.created_at.strftime("%d/%m/%Y %H:%M") %></p>
  

  <% if logged_in? && (comment.user == current_user || comment.article.user == current_user) %>
   <%= link_to 'Excluir Comentário', article_comment_path(article_id: comment.article.id, id: comment.id), data: { turbo_method: :delete, turbo_confirm: 'Tem certeza que deseja excluir este comentário?' }, class: 'btn btn-danger' %>

  <% end %>
</div>
<hr>